import { ReactComponentElement, ReactNode } from "react"
interface AuthProps{
    children : ReactNode;
    id : string;
    password: number;

}


const Auth = (props : AuthProps)  => {
}



export default Auth;